import React from "react";

export default class Dog extends React.Component {
    render() {
        return (
            <div>
                {/* {this.props.dog.map((n,i)=> {
                    return <div key = {i}>{n}</div>
                })} */}
            </div>
        )
    }
}